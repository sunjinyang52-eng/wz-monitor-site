name: Patch Delta Force monitor

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/patch-delta.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo workflow-ok